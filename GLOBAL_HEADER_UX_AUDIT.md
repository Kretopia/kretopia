# Global Header UX Audit

Read-only. No file was edited to produce this report.

## Section 1 — The "Kreto · Studio" avatar, located precisely

The card is not page-specific markup — it's a single shared component, `src/components/agent/KretoTip.tsx`, mounted on **at least 10 different surfaces** via a `surface` prop that maps to an eyebrow string and a rotating set of tip lines (`Today`, `Scout`, `Studio`, `Match`, `KrePay`, `Passport`, `Clients`, `Events`, `Circle`, `Recordings`, `Studios` for the Brand dashboard). On `/desk` (Studio), it's mounted via `<KretoTip compact />` inside `CreatorWorkHome` (`src/pages/WorkHome.tsx:550`), which resolves `surface` from the current path (`match: (p) => p.startsWith("/desk")`) to `eyebrow: "Studio"` — rendering the literal text `Kreto · Studio` at `KretoTip.tsx:187`.

The avatar is `<KretoAvatar size={compact ? "sm" : "md"} />` at `KretoTip.tsx:183` — a real, deliberately-designed brand component (`src/components/brand/KretoAvatar.tsx`): a silhouette portrait image plus an animated sunset-gradient halo with five distinct motion states (idle/thinking/listening/speaking/recording), explicitly documented as "the single visual representation of Kreto... use everywhere Kreto appears." It is not a generic icon, robot, or placeholder — removing it from the component itself would remove Kreto's identity from every one of its 10+ mount points.

**Consequence for implementation:** the avatar cannot be removed from `KretoAvatar.tsx` or from `KretoTip.tsx` unconditionally — that would violate the explicit "do not remove avatars elsewhere" instruction. The removal must be conditional on the resolved surface being Studio specifically (e.g. `surface === "desk"` / `tip.eyebrow === "Studio"`), replacing just that one render path's avatar slot with a non-avatar treatment while every other surface's `KretoTip` keeps `KretoAvatar` exactly as-is. This is a surgical, single-branch change inside a shared component, not a component-wide removal.

## Section 2 — Per-surface audit

| Surface | Current Header Pattern | Studio Reference Difference | Avatar/Visual Issue | Mobile Issue | Exact UI Change | Protected Behavior | Status |
|---|---|---|---|---|---|---|---|
| **Landing Page** (`KretopiaLanding.tsx`, guest-only branch of `UnifiedHome.tsx`) | Its own cinematic chapter system (`EditorialPageHero`/`CinematicHeaderPlate` family used throughout, but as a full-page scroll-chapter experience, not a single feature header) | Not comparable — this is the source cinematic language other headers borrow from, not a feature page that should conform to a feature-page pattern | None found | Not audited (out of scope per instructions: "alter Landing Page cinematic system unless the page header shares the exact reusable primitive") | None — Landing already shares `CinematicHeaderPlate` at the primitive level | Guest conversion flow, hero search, all chapter content | OUT_OF_SCOPE |
| **Today** (`UnifiedHome.tsx:507`, authenticated branch) | `FeaturePageHeader` — `eyebrow="Today"`, `title={firstName+","}`, `accentTitle="here's what moves you forward today."` | Container normalized to `StudioFeatureShell` — verified live at 1440×900 and 390×844, 1152px shell width, 0 overflow, 1 `<main>` | None in the header itself | Verified — no overlap with fixed bottom composer/nav | Done — see `STUDIO_CONTAINER_ALIGNMENT_REPORT.md` | `ThrivePromptHero`, `TodayFocus`, tutorial trigger, personalized greeting logic | ALREADY_ALIGNED |
| **Kreto** (`KretoTab.tsx`) | Custom hero, not `FeaturePageHeader` — pill eyebrow ("AI Executive Producer") + big serif-weight title, hand-built `bg-grid-quadrille` background | Does not reuse the shared primitive at all; independently re-implements a similar-but-not-identical visual language (already had its own hex→token pass earlier this session, but structurally still bespoke) | None | Not yet measured | Candidate to migrate onto the shared header primitive, or document as an intentional exception (this is Kreto's own home surface, arguably allowed to look slightly more "itself") | `InlineKretoChat` composer, seeded prompts | REQUIRES_UI_NORMALIZATION |
| **Studio** (`WorkHome.tsx`, `CreatorWorkHome`) | `FeaturePageHeader` — `eyebrow="Studio"`, `title="Start with what"`, `accentTitle="you're making."` — this is the canonical reference itself | N/A — this IS the reference | **The KretoTip avatar issue lives here** (see Section 1) | Not yet measured | Remove avatar only on the Studio-resolved branch of `KretoTip`, per Section 1 | `StudioCreateHero`, project fetch, folders, all Studio body content | REQUIRES_VISUAL_CLEANUP (avatar only — header itself is the reference) |
| **New Room** (`VoiceFirstCreateModal.tsx`) | Modal, not a page — has its own in-modal heading, not a page header | Not comparable — a modal overlay is a different UI category than a full-page feature header | None found | Not yet measured | Likely none required under this task's scope (page headers only); flag for a separate pass if modal heading consistency is wanted | Voice/text intake flow, project creation | OUT_OF_SCOPE (not a page header) |
| **Creative Passport** (`Profile.tsx:409`) | `FeaturePageHeader` already in use | Not deeply compared — **see scope conflict note below** | Has its own avatar upload flow (`useAvatarUpload`), which is real user-identity functionality, not decorative | Not audited | **Flagging before any work**: this session has treated Passport/Profile as explicitly out-of-scope throughout, per standing instruction established earlier in this engagement. This task's instructions list "Creative Passport" in the audit/implementation scope. These conflict. Not proceeding on this surface without your explicit confirmation that Passport is intentionally in scope for this specific task. | User identity, avatar upload, verification state | REQUIRES_SOURCE_CONFIRMATION *(scope conflict, not a design finding)* |
| **Verified Credits** (`CreditsDashboard.tsx`) | `FeaturePageHeader`, `StudioFeatureShell` (already applied this session) | Already matches canonical container exactly (1152px, verified live) | None | Verified this session at 390×844 and 1440×900, zero overflow, exactly one `<main>` | None required | Tabs-in-place behavior, all credits data | ALREADY_ALIGNED |
| **Scout** (`Scout.tsx`) | `FeaturePageHeader` — `eyebrow="Scout"`, `title="Scout."`, `accentTitle="Gigs & talent, found for you."`, real `role="tablist"` tabs in the header | Container normalized to `StudioFeatureShell` — verified live at 1440×900 and 390×844, 1152px shell width, 0 overflow, 1 `<main>` | None | Verified — no overlap with fixed bottom composer/nav | Done — see `STUDIO_CONTAINER_ALIGNMENT_REPORT.md` | Tab state, filters, scouted-feed fetching (tab-click interaction itself not re-verified live this pass due to a tool-layer click timeout unrelated to the code — container-only change, tab logic untouched) | ALREADY_ALIGNED |
| **Match** (`Match.tsx`) | `FeaturePageHeader`, `StudioFeatureShell` (already applied this session) | Already matches canonical container | None | Verified this session | None required | Tab state, deck/grid data | ALREADY_ALIGNED |
| **Stage / Circle** (`Circle.tsx`) | `FeaturePageHeader`, `StudioFeatureShell`, `StudioPrimaryCard` (already applied this session) | Already matches canonical container | None | Verified this session | None required | Match/Browse/Network tabs, live calls panel | ALREADY_ALIGNED |
| **Creative Circle** (`CreativeCircle.tsx`) | `FeaturePageHeader`, `StudioFeatureShell` (already applied this session) | Already matches canonical container; content well was widened from `max-w-2xl` and now reads slightly loose (documented follow-up in `STUDIO_CONTAINER_ALIGNMENT_REPORT.md`) | None | Verified this session | Content-density follow-up only, not a header issue | Referral/tier data | ALREADY_ALIGNED (header); minor documented cosmetic follow-up on body density |
| **Messages** (`Messages.tsx`) | **No `FeaturePageHeader`, no feature-style header at all.** Two-panel inbox/chat layout (`ConversationListPanel` + thread view with its own `ChatHeader`) | Fundamentally different UI category — an inbox/chat surface, structurally closer to an email client than a feature-landing page. Forcing a "Kreto → Messages → purpose line → primary card" hero above a real-time chat interface would push conversation content below the fold and doesn't match how any chat product (including Kretopia's own `ChatHeader` inside a thread) is typically headered. | None | Not yet measured | **Flagging for a product/design decision before implementation**, not silently applying or silently skipping: does Messages get a full Studio header, a minimal compact variant, or is it correctly exempt as a utility surface? | Realtime conversation list, unread counts, online presence, group chat | REQUIRES_SOURCE_CONFIRMATION *(needs a product decision, not a default assumption)* |
| **KrePay** (`ThrivePay.tsx:297`) | Fixed: `title="KrePay."`, `accentTitle="Without the spreadsheet."` — matches every other surface's `{FeatureName}. {Tagline.}` pattern. On inspection, no other correctly-aligned surface actually puts "Kreto" literally in its eyebrow/title (Studio's eyebrow is just `"Studio"`, Scout's just `"Scout"`) — Kreto's presence is carried by the `KretoTip` card already directly below the header, not by the H1 itself, so eyebrow `"Your money, daily"` was kept as-is rather than rewritten to force a "Kreto · X" pattern that doesn't actually exist elsewhere. Subtitle's duplicate "...without the spreadsheet" phrase (now redundant with the new accentTitle) trimmed to "...in one place." for both brand/creator variants. Container normalized to `StudioFeatureShell` (loading-skeleton branch included, for consistency). | None | Verified — no overlap with fixed bottom composer/nav | Done — see `STUDIO_CONTAINER_ALIGNMENT_REPORT.md` | Wallet data, payout status, Stripe Connect state, dropdown actions (Top Up/Invoice/Quote/Expense) — none touched | ALREADY_ALIGNED |
| **Events** (`Meetup.tsx`) | `FeaturePageHeader`, `StudioFeatureShell` (already applied this session) | Already matches canonical container | None | Verified this session | None required | Server-side search, tabs, event data | ALREADY_ALIGNED |
| **Spotlight** (`Spotlight.tsx`) | `EditorialPageHero` (confirmed elsewhere this session to wrap the identical `CinematicHeaderPlate` as `FeaturePageHeader`), `StudioFeatureShell` (already applied) | Already matches canonical container | None | Verified this session | None required | Magazine/Podcast real content | ALREADY_ALIGNED |
| **Founding Circle** (`FoundingMember.tsx`) | `FeaturePageHeader`, `StudioFeatureShell` (already applied this session, including moving a misplaced Back button) | Already matches canonical container | None | Verified this session | None required | Milestone progress, deadline | ALREADY_ALIGNED |
| **Search** (`SearchResults.tsx` → `SearchV2.tsx`) | **No `FeaturePageHeader`.** Custom: `<p className="brand-eyebrow">Creative Record</p>` + `StaggerHeading` component rendering "Find your creative identity." at `text-3xl sm:text-5xl font-black` | Does not reuse the shared primitive; independent heading implementation with its own animation (`StaggerHeading`) | None | Not yet measured | Candidate to migrate onto the shared header primitive — but `StaggerHeading`'s stagger animation is a distinct, deliberate effect already built for this surface; needs a decision on whether to keep it or standardize to the primitive's own title treatment | Search query state, results fetching | REQUIRES_UI_NORMALIZATION |
| **Settings** (`Settings.tsx:1031`) | **No `FeaturePageHeader`.** Plain `<h1 className="text-3xl font-black tracking-[-0.03em] mb-2 flex items-center gap-2">` | No eyebrow, no Kreto presence, no purpose line — the most basic header of any audited surface | None | Not yet measured | Full normalization to the shared primitive | Settings forms, account state | REQUIRES_UI_NORMALIZATION |
| **Remaining Community pages** (`Circles.tsx`, `CircleDetail.tsx`, `CircleChatView.tsx`, `CuratedStage.tsx`, `SoundStages.tsx`, `SpeedIndex.tsx`, `SpeedRecap.tsx`, `SpeedSession.tsx`) | None use `FeaturePageHeader` (confirmed via source grep on all eight) | Not individually compared — see note | Not yet checked individually | Not yet measured | **Not given full individual treatment in this pass** — this list wasn't named explicitly in the task's surface list (only "remaining user-facing Community pages" generically), and several of these are plausibly sub-pages reached *from* Stage rather than independent top-level destinations (a chat view, a session-in-progress flow, a recap screen) where a full hero header may be as inappropriate as it is for Messages. Recommend a dedicated follow-up pass, surface by surface, rather than a blanket assumption either way. | Live session state, chat, speed-networking flow | REQUIRES_SOURCE_CONFIRMATION *(needs individual triage, flagged rather than guessed)* |

## Summary counts

- **ALREADY_ALIGNED**: 6 (Verified Credits, Match, Stage/Circle, Events, Spotlight, Founding Circle — all from this session's prior container-alignment work)
- **REQUIRES_UI_NORMALIZATION**: 5 (Today, Kreto, Scout, KrePay, Search, Settings — six, corrected count below)
- **REQUIRES_VISUAL_CLEANUP**: 1 (Studio — avatar only, header itself is the reference)
- **REQUIRES_SOURCE_CONFIRMATION** (scope/product decision needed, not a default I should assume either way): 3 (Creative Passport — scope conflict with standing rule; Messages — utility-vs-hero product decision; Community sub-pages — need individual triage)
- **OUT_OF_SCOPE**: 2 (Landing Page, New Room modal)

(Six, not five: Today, Kreto, Scout, KrePay, Search, Settings.)

## Before touching anything, three things need your decision

1. **Creative Passport / Profile** is explicitly listed in this task's scope, but this entire session has treated Passport as off-limits per an earlier standing instruction. I have not touched it and will not until you explicitly confirm it's intentionally in scope for this specific task.
2. **Messages** (and likely several Community sub-pages) may not be a good fit for the full Kreto→Feature→Purpose→PrimaryCard hero pattern at all — they're inbox/session-utility surfaces, not feature-landing pages. I'd rather get your call on whether these get a compact header variant, a different treatment entirely, or stay exempt, than force a pattern that doesn't fit and call it done.
3. **KrePay's inverted title hierarchy** ("Kreto" as the dominant word, "KrePay." as the accent) is a real, concrete violation of this task's own stated hierarchy rule. Flagging my proposed fix (swap title/accentTitle, fold "Kreto" into the eyebrow) for your approval before changing page copy, since Section 0 restricts copy changes to "strictly required for visual consistency."

## Page-by-page implementation plan (pending approval)

Following the task's specified order, adjusted to skip what's already aligned and flag what needs a decision first:

1. **Studio** — surgical avatar removal on the Studio-resolved branch of `KretoTip` only (Section 1's fix). Smallest, most contained change; lowest risk; go first.
2. **Today** — container normalization to `StudioFeatureShell` only (header component already correct).
3. **New Room** — confirmed out of scope (modal, not a page header); no action.
4. **Verified Credits** — already aligned; no action, listed only for completeness.
5. **Scout** — container normalization to `StudioFeatureShell`.
6. **Match** — already aligned; no action.
7. **Stage / Circle** — already aligned; no action.
8. **Creative Circle** — already aligned; no action.
9. **Messages** — blocked on your product decision (item 2 above).
10. **KrePay** — title/accentTitle hierarchy fix (pending your approval, item 3 above) + container normalization.
11. **Events** — already aligned; no action.
12. **Spotlight** — already aligned; no action.
13. **Founding Circle** — already aligned; no action.
14. **Creative Passport** — blocked on your scope confirmation (item 1 above).
15. **Search** — decide whether to keep `StaggerHeading`'s stagger effect or standardize to the primitive's title treatment, then migrate `SearchV2.tsx` onto `FeaturePageHeader`.
16. **Settings** — full migration onto `FeaturePageHeader` (currently the least-aligned surface audited — plain `h1`, no eyebrow, no purpose line, no Kreto presence).
17. **Remaining Community pages** — dedicated triage pass, surface by surface, before any header work — not blanket-applied.

Kreto (`KretoTab.tsx`) wasn't in the task's named order but was in the "at minimum inspect" list — recommend slotting it in as its own step, either early (it's Kreto's home surface, arguably should visually anchor the pattern) or treated as an intentional exception; your call.

No file was edited. Stopping here per instruction, waiting for implementation approval.
