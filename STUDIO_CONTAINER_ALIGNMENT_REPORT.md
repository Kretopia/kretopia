# Studio Container Alignment Report

Read-only source comparison. No feature files were edited to produce this report.

## Canonical Studio reference (verified from source)

Source: `src/pages/WorkHome.tsx` — both `CreatorWorkHome` (line 549) and `BrandWorkHome` (line 217), the two real render paths behind `/desk`.

```
Header:   FeaturePageHeader — full-bleed, sits above the body container entirely
Body:     <div className="max-w-6xl mx-auto px-4 pt-4 pb-36 md:pb-12 space-y-5">   (Creator)
          <div className="max-w-6xl mx-auto px-4 pt-4 pb-24 md:pb-12 space-y-5">   (Brand)
```

- Max width: `max-w-6xl` (1152px), flat at every breakpoint — no `container` utility involved.
- Horizontal padding: `px-4` (16px), flat at every breakpoint — no `sm:`/`md:` escalation.
- Top padding: `pt-4`.
- Bottom padding: asymmetric — `pb-36`/`pb-24` on mobile (bottom-nav clearance), collapsing to `pb-12` at `md:`.
- Vertical rhythm between sections: `space-y-5`.
- Tailwind's own `container` utility (`theme.container` in `tailwind.config.ts`) is a **different, unrelated construct** — `center: true, padding: "2rem"`, unbounded width until `2xl: 1400px`. Studio's real body container never uses it. Any surface using `container` is not using Studio's canonical shape, regardless of what `max-w-*` is layered on top of it, because `container`'s own 2rem (32px) padding stacks with whatever explicit `px-*` is also applied.

## Existing shared primitives (already built, this session — confirmed via file timestamps, Aug 28 09:46–09:48)

`src/components/studio-reference/`: `StudioPrimaryCard.tsx`, `StudioSectionTabs.tsx`, `StudioEmptyState.tsx` all exist and are real, working, Studio-grammar components (flat `rounded-2xl border-border bg-card`, no arbitrary glow). None of them own outer-page-container opinions — they are content-level primitives, not the page shell. **The page-level container/shell primitive (`StudioFeatureShell` or equivalent) does not exist yet.** This is the actual gap Section 3 needs to close.

## Seven-surface comparison

| Surface | Current wrapper/classes | Studio canonical wrapper/classes | Exact change required | Header/body split correct? | Mobile bottom padding | Nested container conflict | Status |
|---|---|---|---|---|---|---|---|
| Stage (`src/pages/Circle.tsx:298`) | `<div className="container mx-auto px-3 sm:px-4 py-4 space-y-4">` | `max-w-6xl mx-auto px-4 pt-4 pb-36 md:pb-12 space-y-5` | Drop `container` (removes the stacked 2rem padding + unbounded-width-until-1400px behavior); replace with flat `max-w-6xl`; change `px-3 sm:px-4` → flat `px-4`; split `py-4` into `pt-4` + asymmetric `pb-*`; `space-y-4` → `space-y-5` | Yes — `FeaturePageHeader` (line 290) sits above this div, full-bleed, matching canonical split | `pb-28 sm:pb-24 md:pb-8` is set on the outer `min-h-screen` wrapper (line 284), not the body container itself — inconsistent placement vs. Studio's pattern of putting `pb-*` directly on the body container | Yes — `container` and `max-w-6xl` are not both present here, but `container`'s own unbounded-width-until-1400px is itself a conflict with the intended fixed 1152px cap | REQUIRES_ALIGNMENT |
| Creative Circle (`src/pages/CreativeCircle.tsx:112`) | `<div className="container mx-auto px-4 pt-4 max-w-2xl space-y-6">` | `max-w-6xl mx-auto px-4 pt-4 pb-36 md:pb-12 space-y-5` | Drop `container`; `max-w-2xl` (672px) → `max-w-6xl` (1152px) — this is the widest gap of the seven surfaces, the content well is currently 42% of Studio's real width; no `pb-*` visible on this line, needs confirmation; `space-y-6` → `space-y-5` | Yes — `FeaturePageHeader` (line 104) precedes this div | Not visible in the grepped line — REQUIRES_SOURCE_CONFIRMATION | Yes — `container` stacked with `max-w-2xl` simultaneously | REQUIRES_ALIGNMENT |
| Match (`src/pages/Match.tsx:93,97`) | Two separate divs: `<div className="max-w-2xl mx-auto px-4 pt-3">` then `<div className="max-w-2xl mx-auto px-3 py-3">` | `max-w-6xl mx-auto px-4 pt-4 pb-36 md:pb-12 space-y-5` | Collapse the two separate `mx-auto` wrappers into one; `max-w-2xl` → `max-w-6xl`; reconcile the inconsistent `px-4`/`px-3` between the two current divs; add explicit asymmetric `pb-*` | Yes — `FeaturePageHeader` (line 85) precedes both | Only `pb-28` on the outer `min-h-screen` wrapper (line 80), same pattern as Stage | **Yes — two independent `max-w-2xl mx-auto` divs stacked back-to-back is a real nested-container-style redundancy**, even though neither is literally nested inside the other | REQUIRES_ALIGNMENT |
| Events (`src/pages/Meetup.tsx:226`) | `<div className="container mx-auto max-w-5xl px-4 py-6">` | `max-w-6xl mx-auto px-4 pt-4 pb-36 md:pb-12 space-y-5` | Drop `container`; `max-w-5xl` (1024px) → `max-w-6xl` (1152px); split `py-6` into `pt-4` + asymmetric `pb-*`; no `space-y-*` present, needs adding | Yes — `FeaturePageHeader` (line 188) precedes this div | Only `pb-24` on the outer `min-h-screen` wrapper (line 181) | Yes — `container` + `max-w-5xl` stacked | REQUIRES_ALIGNMENT |
| Spotlight (`src/pages/Spotlight.tsx:79`) | `<div className="mx-auto max-w-5xl px-4 sm:px-6 py-10 space-y-10">` | `max-w-6xl mx-auto px-4 pt-4 pb-36 md:pb-12 space-y-5` | No `container` stacking (clean on that front) — but `max-w-5xl` → `max-w-6xl`; `px-4 sm:px-6` escalation → flat `px-4`; `py-10` → `pt-4` + asymmetric `pb-*`; `space-y-10` → `space-y-5` (currently double Studio's rhythm) | Yes — `EditorialPageHero` (line 71) precedes this div, confirmed in the earlier audit to wrap the same `CinematicHeaderPlate` as `FeaturePageHeader` | Not present — this surface has no mobile-bottom-nav-clearance padding at all | No stacked container class, but every spacing value diverges from canonical | REQUIRES_ALIGNMENT |
| Founding Circle (`src/pages/FoundingMember.tsx:43,61`) | Pre-header: `<div className="max-w-2xl mx-auto px-4 pt-4">` (line 43, before `FeaturePageHeader` at line 53); post-header body: `<div className="max-w-2xl mx-auto px-4 pt-4 pb-24">` (line 61) | `max-w-6xl mx-auto px-4 pt-4 pb-36 md:pb-12 space-y-5` | Needs source read of lines 43–61 to confirm what the pre-header div actually contains before deciding whether it should be removed, merged into the header, or is a legitimate exception; body div: `max-w-2xl` → `max-w-6xl`; `pb-24` (fixed) → asymmetric `pb-36 md:pb-12`; no `space-y-*` visible | **REQUIRES_SOURCE_CONFIRMATION** — a container renders before the header, which is not the canonical pattern (header should be full-bleed, first) | Fixed `pb-24`, no `md:` reduction — diverges from Studio's responsive collapse | Two separate `max-w-2xl mx-auto` divs, one before and one after the header — same redundancy pattern as Match | REQUIRES_SOURCE_CONFIRMATION |
| Verified Credits (`src/pages/CreditsDashboard.tsx:172`) | `<main className="container relative mx-auto max-w-3xl px-4 pt-6">` | `max-w-6xl mx-auto px-4 pt-4 pb-36 md:pb-12 space-y-5` | Drop `container`; `max-w-3xl` (768px) → `max-w-6xl` (1152px) — second-widest gap after Creative Circle; `pt-6` → `pt-4`; no `pb-*`/`space-y-*` visible on this line, needs confirmation | Yes — `FeaturePageHeader` (line 165) precedes this `<main>` | Not visible in the grepped line — REQUIRES_SOURCE_CONFIRMATION | Yes — `container` + `max-w-3xl` stacked, plus this is the only surface using `<main>` instead of `<div>` for the body wrapper (not itself wrong, but inconsistent with the other six) | REQUIRES_ALIGNMENT |

## Summary

Zero of the seven surfaces currently match Studio's canonical container exactly. Every surface either (a) stacks Tailwind's `container` utility with an explicit `max-w-*` — a real, structural double-padding bug, not just a style preference, since `container`'s own 2rem padding adds to whatever `px-*` is also declared — or (b) uses a `max-w-*` narrower than `max-w-6xl` (ranging from `max-w-2xl` at 672px up to `max-w-5xl` at 1024px, against Studio's 1152px), or both. Two surfaces (Match, Founding Circle) have a second redundant `mx-auto` wrapper rather than one clean container. Founding Circle additionally renders a container *before* the header, which needs a source read to classify correctly rather than assumed. No surface currently reproduces Studio's asymmetric mobile-bottom-nav-clearance padding (`pb-36 md:pb-12` / `pb-24 md:pb-12`) on the body container itself — where bottom padding exists at all, it is on the outer `min-h-screen` wrapper instead, which is a different (not necessarily wrong, but inconsistent) placement.

No `ALIGNED` status was assigned to any surface — none currently reproduce Studio's exact values.

## Implementation — before/after evidence

`StudioFeatureShell` created at `src/components/studio-reference/StudioFeatureShell.tsx`, encoding the canonical classes verbatim: `max-w-6xl mx-auto px-4 pt-4 pb-36 md:pb-12 space-y-5`. Applied to all seven surfaces. Verified live via `document.querySelectorAll` + `getBoundingClientRect` (not just visual screenshot) at 1440×900 and 390×844 for every surface: shell width exactly 1152px at desktop, full viewport width (minus padding) at mobile, `document.body.scrollWidth === document.body.clientWidth` (zero horizontal overflow) on all seven at both sizes.

| Surface | Before body container | After body container | Header/body split | Desktop verified | Mobile verified | Regression risk | Status |
|---|---|---|---|---|---|---|---|
| Stage (`Circle.tsx`) | `<div className="container mx-auto px-3 sm:px-4 py-4 space-y-4">`; outer wrapper also carried `pb-28 sm:pb-24 md:pb-8` | `<StudioFeatureShell>`; outer wrapper's redundant `pb-*` removed (shell now owns the only bottom padding) | Correct — `FeaturePageHeader` full-bleed above | 1152px width, 0 overflow, 1 `<main>` (app-shell's) | 390px width, 0 overflow | None found | ALIGNED |
| Creative Circle (`CreativeCircle.tsx`) | `<div className="container mx-auto px-4 pt-4 max-w-2xl space-y-6">`; outer wrapper `pb-24` | `<StudioFeatureShell>`; outer wrapper's redundant `pb-24` removed | Correct | 1152px width, 0 overflow | 390px width, 0 overflow | Content well widened from 672px to 1152px — visually looser on desktop since inner content (cards, copy) wasn't authored for the wider well; no functional regression, flagging as a cosmetic follow-up | ALIGNED_WITH_EXCEPTION |
| Match (`Match.tsx`) | Two separate `max-w-2xl mx-auto` divs (`px-4 pt-3` then `px-3 py-3`); outer wrapper `pb-28` | Collapsed into one `<StudioFeatureShell>`; outer wrapper's redundant `pb-28` removed | Correct | 1152px width, 0 overflow | 390px width, 0 overflow | Same content-well-widening note as Creative Circle | ALIGNED_WITH_EXCEPTION |
| Events (`Meetup.tsx`) | `<div className="container mx-auto max-w-5xl px-4 py-6">`; outer wrapper `pb-24` | `<StudioFeatureShell>`; `KretoTip`'s redundant `mb-5` removed (shell's own `space-y-5` already provided that gap — kept would have double-spaced); outer wrapper's redundant `pb-24` removed | Correct | 1152px width, 0 overflow | 390px width, 0 overflow | None found | ALIGNED |
| Spotlight (`Spotlight.tsx`) | `<div className="mx-auto max-w-5xl px-4 sm:px-6 py-10 space-y-10">` | `<StudioFeatureShell>` | Correct — `EditorialPageHero` full-bleed above (confirmed elsewhere to wrap the same `CinematicHeaderPlate` as `FeaturePageHeader`) | 1152px width, 0 overflow | 390px width, 0 overflow | Vertical rhythm halved (`space-y-10` → `space-y-5`) — intentional normalization, confirmed visually still reads as two distinct sections, not cramped | ALIGNED |
| Founding Circle (`FoundingMember.tsx`) | Pre-header `<div className="max-w-2xl mx-auto px-4 pt-4">` wrapping a Back button (rendered *before* `FeaturePageHeader` — confirmed via direct source read, not assumed); post-header `<div className="max-w-2xl mx-auto px-4 pt-4 pb-24">` | Pre-header div removed entirely; Back button moved inside `<StudioFeatureShell>` as the first child, after the header | **Fixed** — was incorrect (content before the header), now correct | 1152px width, 0 overflow; screenshot confirms Back renders below the header pill/title | 390px width, 0 overflow; screenshot confirms same placement, no overlap with the fixed bottom composer/nav | None found — click behavior (`navigate(-1)`) unchanged, only position moved | ALIGNED |
| Verified Credits (`CreditsDashboard.tsx`) | `<main className="container relative mx-auto max-w-3xl px-4 pt-6">`; outer wrapper `pb-24` | `<StudioFeatureShell className="relative">` (plain `div`, **not** `as="main"`) | Correct | 1152px width, 0 overflow, **exactly 1 `<main>` landmark** | 390px width, 0 overflow, 1 `<main>` | Tab-switch scroll position confirmed unchanged (`window.scrollY` identical before/after clicking a tab) | ALIGNED |

### Deviation from instruction, with evidence

Section 5 of the implementation instructions specified `<StudioFeatureShell as="main">` for Verified Credits to preserve its `<main>` landmark. Live inspection (`document.querySelectorAll('main').length`) showed this produced **two** `<main>` elements on the page — the app-shell already renders one globally (`<main className="pb-36 lg:pb-0">`, confirmed present identically on Stage, which never had its own `<main>`). Two `<main>` landmarks on one document is an invalid nested-landmark violation, which the same instructions separately required checking for ("no invalid nested `<main>`"). Kept the `as` prop on the primitive itself (still available for a future page that genuinely lacks an app-level landmark) but did not use it here — `StudioFeatureShell` on Credits renders as a plain `div`, same as the other six surfaces. Verified the fix: exactly 1 `<main>` on the page afterward.

### Known follow-up, not blocking

Creative Circle and Match's content was authored for a narrower `max-w-2xl` well; widening to Studio's `max-w-6xl` is structurally correct (matches the canonical container) but leaves both surfaces' inner content visually looser than Studio's own dense grid layouts, since neither page's internal grid/spacing was redesigned for the wider well. This is a content-layout follow-up, not a container-alignment defect — flagging honestly rather than calling either surface fully finished cosmetically.

## Eighth surface — Today, added under the Global Header UX task

| Surface | Before body container | After body container | Header/body split | Desktop verified | Mobile verified | Regression risk | Status |
|---|---|---|---|---|---|---|---|
| Today (`src/components/home/UnifiedHome.tsx:514`, authenticated branch of `UnifiedHome`) | Two nested divs: `<div className="mx-auto px-3 sm:px-4 pt-4 max-w-7xl">` wrapping `<div className="space-y-5">` | Collapsed into one `<StudioFeatureShell>` | Correct — `FeaturePageHeader` (line 507) full-bleed above, unchanged | 1152px width, 0 overflow, 1 `<main>` (app-shell's); screenshot confirms header, prompt card, Today Focus panel all align to the same width | 390px width, 0 overflow; screenshot confirms same, no overlap with fixed bottom composer/nav | None found — outer wrapper (`bg-background min-h-screen accent-passport`) has no `pb-*` of its own, so no duplicate-padding cleanup was needed here (unlike Stage/Match/Events/Credits) | ALIGNED |
