# Kretopia Platform Overhaul — Execution Plan

Goal: push the new Kretopia brand (Satoshi type, Midnight + Sunset gradient palette, lowercase `kretopia` wordmark, Kreto as the AI Executive Producer) across every surface without breaking infrastructure (routes, tables, edge functions stay named as-is).

---

## Phase 1 — Kreto Identity (this turn)

1. **Generate Kreto avatar** — premium-quality silhouette portrait, magenta→coral glow rim, midnight backdrop, transparent PNG. Saved to `src/assets/kreto-avatar.png`.
2. **Landing page "Meet Kreto" section** — drop the avatar into `KretopiaLanding.tsx` with a soft animated halo (purple→magenta→coral pulse), supporting copy ("Your AI Executive Producer"), and a single CTA.
3. **Reusable `<KretoAvatar />` component** so the same asset/halo is used everywhere Kreto appears (FAB, drawer header, briefing cards).

## Phase 2 — Global Chrome (next turn)

Surfaces every authed user sees on every page.

- **Top nav + bottom nav** — repaint with Midnight surface, Satoshi labels, Sunset gradient on the active indicator. New BrandLogo already lands here.
- **Auth screen** — replace `AuthBrandingPanel` with Kretopia narrative: hero gradient, "Where Creativity Lives", Kreto teaser, Satoshi headlines.
- **Hamburger drawer + notifications panel** — Midnight surfaces, swap "Thrive" copy → `BRAND.*` lexicon.
- **Loading + empty states** — BrandDots already there; verify they pick up the new gradient tokens.

## Phase 3 — Kreto Agent Surfaces

Anywhere the AI speaks today is "Thrive". Sweep:

- `ThriveAgentFab` → `KretoFab` (visual + label)
- Agent drawer header, proactive cards, voice composer, doc-engine "generated by" labels
- `thrive-document-engine` system prompt voice → KRETO_VOICE (Executive Producer, no "AI generated" phrasing)
- Replace ✨/robot iconography with the `<KretoAvatar />` mark + signal ring

No edge function renames — only prompt + UI changes.

## Phase 4 — Pillar Destinations

Each major page gets a cinematic header (gradient halo, Satoshi display headline, ownable lexicon):

- `/passport` → "Creative Passport"
- `/desk` → "Studio"
- `/scout` → "Scout" (yellow accent retained)
- `/match` → "Match" (magenta accent)
- `/thrivepay` → "KrePay" (money-green retained)
- `/circle` → "ThriveIN" (events + community sub-brand framing)

## Phase 5 — Polish & Hygiene

- Audit hardcoded `indigo-*` / old `signal-*` hex → semantic tokens
- Update transactional email templates (welcome, application, event) to Kretopia voice + wordmark
- Update SEO/OG/meta sweep beyond landing (auth, /scout, /passport public pages)
- Refresh README, public/llms.txt with new positioning

---

## Technical guardrails (locked)

- **Do NOT rename**: routes (`/thrive/*`), tables (`thrive_documents`, `thrive_memory`), edge functions (`thrive-ai-chat`, `thrive-document-engine`, `thrive-voice-turn`), storage buckets, RPC names. UI labels pull from `src/lib/brandLexicon.ts`.
- **Tokens only** — no hardcoded hex. New `--k-*` tokens already in `index.css`.
- **Vibe default** stays Midnight; Daylight/Neon remain opt-in.
- **Money-green** stays reserved for KrePay surfaces only.

---

## What I'll ship this turn

1. Generate the Kreto avatar asset (premium tier, transparent PNG).
2. Create `src/components/brand/KretoAvatar.tsx` (avatar + animated halo).
3. Wire it into the "Meet Kreto" section of `KretopiaLanding.tsx`.

Then we pick Phase 2 (Global Chrome) or jump to Phase 3 (Kreto agent sweep) next.
